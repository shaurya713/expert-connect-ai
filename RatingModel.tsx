"use client";
import { useState } from "react"; import { Star } from "lucide-react";
export default function RatingModel({ onSubmit }: { onSubmit?: (rating: number, feedback: string) => void }) { const [rating,setRating]=useState(0); const [feedback,setFeedback]=useState(""); return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">Rate service</h3><div className="mt-4 flex gap-2">{[1,2,3,4,5].map(v=><button key={v} type="button" onClick={()=>setRating(v)} className="rounded-lg p-1"><Star size={24} className={v<=rating?"fill-amber-400 text-amber-400":"text-slate-300"}/></button>)}</div><textarea className="input mt-4 min-h-24" placeholder="Feedback" value={feedback} onChange={e=>setFeedback(e.target.value)}/><button onClick={()=>onSubmit?.(rating,feedback)} className="mt-4 rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white">Submit rating</button></div>; }
