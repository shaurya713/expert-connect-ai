"use client";
import { useDropzone } from "react-dropzone";
import { UploadCloud, X } from "lucide-react";
export function FileDropzone({ files, onChange }: { files: File[]; onChange: (files: File[]) => void }) { const { getRootProps, getInputProps, isDragActive } = useDropzone({ accept: { "image/*": [], "video/*": [], "audio/*": [] }, multiple: true, onDrop: (accepted) => onChange([...files, ...accepted]) }); return <div className="space-y-3"><div {...getRootProps()} className={`cursor-pointer rounded-[1.25rem] border border-dashed p-6 text-center transition ${isDragActive ? "border-teal-400 bg-teal-400/10" : "border-slate-300/90 bg-white/45 hover:bg-white/70 dark:border-white/15 dark:bg-white/[.04]"}`}><input {...getInputProps()}/><div className="mx-auto mb-3 grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-teal-200 via-sky-200 to-violet-200 text-slate-950"><UploadCloud size={24}/></div><p className="text-sm font-black">Upload media</p><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">Drag files here or click to browse</p></div>{files.length > 0 && <div className="grid gap-2 sm:grid-cols-2">{files.map((file, index) => <div key={`${file.name}-${index}`} className="flex items-center justify-between rounded-2xl border border-slate-200/70 bg-white/65 px-3 py-2 text-sm"><span className="truncate pr-3">{file.name}</span><button type="button" onClick={() => onChange(files.filter((_, i) => i !== index))} className="rounded-xl p-1 hover:bg-slate-100" aria-label="Remove file"><X size={16}/></button></div>)}</div>}</div>; }
