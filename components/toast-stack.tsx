"use client";
import { X } from "lucide-react";
export type Toast = { id: string; message: string; type: "success" | "error" | "info" };
export function ToastStack({ toasts, onClose }: { toasts: Toast[]; onClose: (id: string) => void }) { return <div className="fixed right-4 top-4 z-50 grid w-[min(92vw,380px)] gap-2">{toasts.map((toast) => <div key={toast.id} className={`flex items-start justify-between gap-3 rounded-2xl border p-4 shadow-2xl backdrop-blur-xl ${toast.type === "error" ? "border-rose-300/70 bg-rose-50/90 text-rose-800 dark:border-rose-400/20 dark:bg-rose-950/80 dark:text-rose-100" : toast.type === "success" ? "border-emerald-300/70 bg-emerald-50/90 text-emerald-800 dark:border-emerald-400/20 dark:bg-emerald-950/80 dark:text-emerald-100" : "border-sky-300/70 bg-sky-50/90 text-sky-800 dark:border-sky-400/20 dark:bg-sky-950/80 dark:text-sky-100"}`}><p className="text-sm font-bold leading-5">{toast.message}</p><button type="button" onClick={() => onClose(toast.id)} className="rounded-lg p-1 hover:bg-black/5 dark:hover:bg-white/10" aria-label="Close toast"><X size={15}/></button></div>)}</div>; }
