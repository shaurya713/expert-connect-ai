"use client";
import { useState } from "react"; import { resetPassword } from "@/services"; import { Brand, Card, PageShell } from "@/components/ui-kit";
export default function ResetPasswordPage(){ const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const token=new URLSearchParams(window.location.search).get("token")||""; try{const res=await resetPassword(token,password); setMessage(res.message||"Password reset successful");}catch(err){setMessage(err instanceof Error?err.message:"Reset failed");}} return <PageShell><div className="grid min-h-screen place-items-center px-4"><Card className="w-full max-w-md p-6"><div className="mb-8"><Brand/></div><h1 className="text-3xl font-black">Reset password</h1><form onSubmit={submit} className="mt-6 space-y-4"><input className="input" type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/><button className="w-full rounded-2xl bg-slate-950 px-5 py-3 font-black text-white">Update password</button></form>{message&&<p className="mt-4 text-sm text-slate-600">{message}</p>}</Card></div></PageShell> }
